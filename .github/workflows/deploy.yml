name: deploy
on:
  push:
    branches:
    - main
  release:
    types:
    - created

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: ./.github/workflows/build
      - run: pnpm run publish
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}